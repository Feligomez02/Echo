{
  "buildCommand": "npm install --legacy-peer-deps && prisma generate && next build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/scrape",
      "schedule": "0 2 * * 0"
    }
  ]
}
