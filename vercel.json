{
  "framework": "nextjs",
  "installCommand": "npm ci --legacy-peer-deps && npx prisma generate",
  "buildCommand": "npm run build",
  "functions": {
    "app/api/**/*.js": {
      "includeFiles": "node_modules/@prisma/client/**"
    }
  },
  "crons": [
    {
      "path": "/api/cron/scrape",
      "schedule": "0 2 * * 0"
    }
  ]
}
