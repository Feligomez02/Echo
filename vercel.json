{
  "buildCommand": "npm ci --legacy-peer-deps && prisma generate && next build",
  "framework": "nextjs",
  "installCommand": "npm ci --legacy-peer-deps",
  "crons": [
    {
      "path": "/api/cron/scrape",
      "schedule": "0 2 * * 0"
    }
  ]
}
