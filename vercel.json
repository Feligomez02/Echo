{
  "buildCommand": "npx prisma generate && next build",
  "framework": "nextjs",
  "installCommand": "npm ci --legacy-peer-deps --ignore-scripts",
  "crons": [
    {
      "path": "/api/cron/scrape",
      "schedule": "0 2 * * 0"
    }
  ]
}
